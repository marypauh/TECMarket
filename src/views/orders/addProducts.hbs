<div class="row">
    {{#each products}}
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title d-flex justify-content-between align-items-center">
                   <b> Product </b>
                </h3>
                <p>Id Orden: {{newOrder.state}} </p>
                <p>Id Product: {{idProduct}}</p>
                <p>Name: {{name}}</p>
                <p>Description: {{description}}</p> 
               
                <div class="form-group">
                     <input type="Number" name="quant" class="form-control" placeholder="Quantity" autofocus/>
                </div>
 
                <form action="/orders/addP" method="POST">
                    <input type="hidden" name="_method" value = "DELETE">
                        <button type="submit" class="btn btn-danger btn-block btn-sm">
                            Purchase 
                        </button>
                </form> 
                 <form action="/product/delete/{{_id}}?_method=DELETE" method="POST">
                    <input type="hidden" name="_method" value = "Holi">
                        <button type="submit" class="btn btn-danger btn-block btn-sm">
                            Ready
                        </button>
                </form> 
            </div>
        </div>
    </div>
    {{/each}}
</div>
